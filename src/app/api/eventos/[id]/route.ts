// GET /api/eventos/[id] — devuelve un evento por ID.
// PUT /api/eventos/[id] — UNICO punto de mutacion del estado del evento.
// Toda mutacion de estado del sistema pasa exclusivamente por este endpoint.
export async function GET() {}
export async function PUT() {}
